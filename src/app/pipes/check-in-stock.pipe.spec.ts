import { CheckInStockPipe } from './check-in-stock.pipe';

describe('CheckInStockPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckInStockPipe();
    expect(pipe).toBeTruthy();
  });
});
